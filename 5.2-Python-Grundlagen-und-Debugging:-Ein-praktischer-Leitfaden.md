# Python Grundlagen und Debugging: Ein praktischer Leitfaden

## 1. Projekt-Setup über GitHub

1. Erstellen Sie ein neues Repository auf GitHub.
2. Klonen Sie das Repository mit GitHub Desktop:
   - Öffnen Sie GitHub Desktop
   - Klicken Sie auf "File" > "Clone Repository"
   - Wählen Sie Ihr neues Repository aus und klicken Sie auf "Clone"

## 2. Python-Installation und virtuelle Umgebung

Bevor wir mit dem Coding beginnen, installieren wir Python 3.11 mithilfe von uv und erstellen eine virtuelle Umgebung.

### Python 3.11 Installation mit uv

1. Öffnen Sie das Terminal in Visual Studio Code (View > Terminal oder ``Ctrl+` ``).

2. Installieren Sie uv:

   Für macOS und Linux:
   ```bash
   curl -LsSf https://astral.sh/uv/install.sh | sh
   ```

   Für Windows (in PowerShell):
   ```powershell
   irm https://astral.sh/uv/install.ps1 | iex
   ```

3. Installieren Sie Python 3.11:
   ```bash
   uv python install 3.11
   ```

4. Überprüfen Sie die Installation:
   ```bash
   uv python --version
   ```

### Erstellen einer virtuellen Umgebung

1. Erstellen Sie eine virtuelle Umgebung:
   ```bash
   uv venv -p 3.11
   ```

2. Aktivieren Sie die virtuelle Umgebung:

   Für macOS und Linux:
   ```bash
   source .venv/bin/activate
   ```

   Für Windows:
   ```bash
   .\.venv\Scripts\activate
   ```

## 3. Öffnen des Projekts in VS Code

1. Öffnen Sie VS Code
2. Gehen Sie zu "File" > "Open Folder"
3. Wählen Sie den Ordner Ihres geklonten Repositories aus

## 4. Erstellen einer Python-Datei

1. Klicken Sie in VS Code auf das "New File" Icon
2. Nennen Sie die Datei `buggy_script.py`

## 5. Buggy Code

Kopieren Sie den folgenden Code in `buggy_script.py`:

```python
# Dies ist ein Beispielskript mit verschiedenen Bugs und Grundkonzepten der Python-Programmierung

# Variablen: Behälter für Daten
# Hier definieren wir einige Variablen mit verschiedenen Datentypen
zahl = 10  # Integer (Ganzzahl)
kommazahl = 3.14  # Float (Gleitkommazahl)
text = "Hallo, Welt!"  # String (Zeichenkette)
liste = [1, 2, 3, 4, 5]  # Liste (veränderbare Sequenz von Elementen)

# Funktionen: Wiederverwendbare Codeblöcke
# Bug 1: Fehlender Doppelpunkt nach der Funktionsdefinition
def addiere(a, b)
    # Bug 2: Falsche Einrückung
  return a + b

# Kontrollstrukturen: Steuern den Programmablauf
# If-Anweisung: Bedingte Ausführung von Code
# Bug 3: Falscher Vergleichsoperator
if zahl = 10:
    print("Die Zahl ist 10")
else:
    print("Die Zahl ist nicht 10")

# For-Schleife: Iteration über eine Sequenz
# Bug 4: Falsche Schleifenvariable
for i in range(5)
    print(x)  # Bug 5: Unbenutzter Schleifenzähler

# While-Schleife: Wiederholung, solange eine Bedingung wahr ist
# Bug 6: Endlosschleife
while True:
    print("Dies ist eine Endlosschleife")

# Klassen: Blaupausen für Objekte
class Auto:
    # Bug 7: Fehlender self-Parameter
    def __init__(marke, modell):
        self.marke = marke
        self.modell = modell
    
    # Bug 8: Falsche Methodendefinition
    def fahren()
        print(f"{self.marke} {self.modell} fährt.")

# Fehlerbehandlung: Umgang mit Ausnahmen
# Bug 9: Falsche Ausnahmebehandlung
try:
    resultat = 10 / 0
except:
    print("Ein Fehler ist aufgetreten")
finally
    print("Dies wird immer ausgeführt")

# Hauptprogramm
if __name__ == "__main__":
    # Bug 10: Falscher Funktionsaufruf
    ergebnis = addiere(5)
    print(f"Das Ergebnis ist: {ergebnis}")

    # Objekterstellung und -verwendung
    mein_auto = Auto("VW", "Golf")
    mein_auto.fahren()

print("Programm beendet")
```

## 6. Ausführen des Skripts und Fehleranalyse

Führen Sie das Skript aus, indem Sie in VS Code auf den "Run Python File" Button klicken oder im Terminal eingeben:

```bash
python buggy_script.py
```

Sie werden verschiedene Fehlermeldungen sehen. Lassen Sie uns diese analysieren:

1. `SyntaxError`: Fehlender Doppelpunkt nach der Funktionsdefinition
2. `IndentationError`: Falsche Einrückung in der Funktion
3. `SyntaxError`: Falscher Vergleichsoperator (= statt ==)
4. `SyntaxError`: Fehlender Doppelpunkt nach der for-Schleife
5. `NameError`: Verwendung einer nicht definierten Variable x
6. Endlosschleife (kein Syntaxfehler, aber logischer Fehler)
7. `TypeError`: Fehlender self-Parameter in der Klassenmethode
8. `SyntaxError`: Fehlender Doppelpunkt nach der Methodendefinition
9. `SyntaxError`: Fehlender Doppelpunkt nach finally
10. `TypeError`: Falscher Funktionsaufruf (fehlender Parameter)

## 7. Debugging-Techniken

### Print-Debugging

Eine einfache Methode zur Fehlersuche ist das Einfügen von print-Anweisungen:

```python
print(f"Der Wert von zahl ist: {zahl}")
```

### Logging

Für fortgeschritteneres Debugging können Sie das logging-Modul verwenden:

```python
import logging

logging.basicConfig(level=logging.DEBUG, format='%(asctime)s - %(levelname)s - %(message)s')
logging.debug(f"Der Wert von zahl ist: {zahl}")
```

Der Vorteil von Logging gegenüber print ist, dass es verschiedene Logging-Level bietet und leicht konfigurierbar ist.

## 8. Verwendung des Python Debuggers

1. Setzen Sie Breakpoints, indem Sie links neben die Zeilennummern in VS Code klicken.
2. Klicken Sie auf "Run and Debug" in der Seitenleiste von VS Code.
3. Wählen Sie "Python File" aus dem Dropdown-Menü.
4. Klicken Sie auf den grünen Play-Button oder drücken Sie F5.

Der Debugger stoppt an den Breakpoints. Sie können:
- Variablenwerte inspizieren
- Schrittweise durch den Code gehen (Step Over, Step Into, Step Out)
- Die Ausführung fortsetzen oder abbrechen

## 9. Fehlerbehebung

Hier ist der korrigierte Code:

```python
# Korrigierter Code

def addiere(a, b):
    return a + b

if zahl == 10:
    print("Die Zahl ist 10")
else:
    print("Die Zahl ist nicht 10")

for i in range(5):
    print(i)

counter = 0
while counter < 5:
    print(f"Schleifendurchlauf {counter}")
    counter += 1

class Auto:
    def __init__(self, marke, modell):
        self.marke = marke
        self.modell = modell
    
    def fahren(self):
        print(f"{self.marke} {self.modell} fährt.")

try:
    resultat = 10 / 0
except ZeroDivisionError:
    print("Division durch Null ist nicht erlaubt")
finally:
    print("Dies wird immer ausgeführt")

if __name__ == "__main__":
    ergebnis = addiere(5, 3)
    print(f"Das Ergebnis ist: {ergebnis}")

    mein_auto = Auto("VW", "Golf")
    mein_auto.fahren()

print("Programm erfolgreich beendet!")
```

## Fazit

Gratulation! Sie haben erfolgreich grundlegende Python-Konzepte gelernt und Debugging-Techniken angewendet. Debugging ist ein wesentlicher Teil des Programmierens. Mit Übung werden Sie immer besser darin, Fehler zu finden und zu beheben.

Erinnern Sie sich:
- Lesen Sie Fehlermeldungen sorgfältig.
- Nutzen Sie print-Anweisungen und Logging für einfaches Debugging.
- Verwenden Sie den Python Debugger für komplexere Probleme.
- Üben Sie regelmäßig, um Ihre Fähigkeiten zu verbessern.

Viel Erfolg bei Ihren zukünftigen Python-Projekten!